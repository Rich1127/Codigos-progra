while True:
    entrada = input("Introduce un numero entero: ")
    if entrada.isdigit():
        numero = int(entrada)
        print (f"El numero entero es {numero}.")
        break
    else:
        print ("Error: Entrada no valida, por favor introduce un numero entero")
